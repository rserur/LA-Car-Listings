<%= form_for @manufacturer do |f| %>

 <% if @manufacturer.errors.any? %>

    <p>
      <h2><%= pluralize(@manufacturer.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @manufacturer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </p>

  <% end %>

  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :country %>
    <%= f.text_field :country %>
  </p>

  <%= f.submit %>

<% end %>
